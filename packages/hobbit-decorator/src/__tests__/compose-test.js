/*global describe*/
/*global it*/
/*global expect*/
/*global jest*/

import { compose } from '../compose';

describe('the compose utility function', () => {
    it('should compose multiple functions and call them in the right order', () => {
        const function1 = jest.fn();
    });
});
